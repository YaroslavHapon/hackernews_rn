// Import library to help create the component
import React from 'react'
import { AppRegistry } from 'react-native'
import App from './src/components/App'

// Render into device
AppRegistry.registerComponent('hackernews', () => App);
